name: CI

on:
  pull_request:
    branches: [dev, main]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check Static Project
        run: 'echo "CI OK: proyecto frontend estático (sin build Node)"'